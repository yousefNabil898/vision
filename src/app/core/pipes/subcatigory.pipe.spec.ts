import { SubcatigoryPipe } from './subcatigory.pipe';

describe('SubcatigoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SubcatigoryPipe();
    expect(pipe).toBeTruthy();
  });
});
