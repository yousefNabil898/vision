import { CartProductsPipe } from './cart-products.pipe';

describe('CartProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new CartProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
